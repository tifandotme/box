{
  "scripts": {
    "typecheck": "tsc -p tsconfig.mise-tasks.json",
    "lint": "oxlint .mise/tasks",
    "format": "oxfmt --write .mise/tasks",
    "format:check": "oxfmt --check .mise/tasks"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "oxfmt": "^0.55.0",
    "oxlint": "^1.70.0",
    "typescript": "^6.0.3"
  }
}
